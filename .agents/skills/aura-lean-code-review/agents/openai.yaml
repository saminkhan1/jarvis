interface:
  display_name: "AURA Lean Code Review"
  short_description: "Find AURA code that should not exist yet"
  default_prompt: "Review the current AURA diff for lean architecture, Hermes boundary drift, and startup engineering waste."
policy:
  allow_implicit_invocation: true
